# Python 3.9 or newer. 3.9.6 is the version the whole chain was verified on,
# on macOS. Linux has never been run.
# Everything the toolchain imports beyond the standard library.
# ffmpeg and ffprobe are separate: they are binaries, not packages.
numpy>=1.24
pillow>=10.0
requests>=2.31
# macOS's system python 3.9 is built on LibreSSL, and urllib3 2 warns about it on
# every run. urllib3 1.26 is fully supported by requests and stays quiet.
urllib3<2; python_version < "3.10"

# Optional, and only one of these two is needed:
#   ElevenLabs Scribe  -> nothing to install, set ELEVENLABS_API_KEY
#   local transcription -> bash scripts/setup.sh --local-asr  (faster-whisper, into the venv)
